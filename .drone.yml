kind: pipeline
name: default

steps:
- name: Install
  image: node:11-alpine
  commands:
  - npm ci
  when:
    branch:
      - infra
    event:
      - push

- name: Build
  image: node:11-alpine
  commands:
  - npm run build
  when:
    branch:
      - infra
    event:
      - push

- name: Github Release
  image: plugins/github-release
  settings:
    api_key:
      from_secret: github_auth
    files:
      - dist/*
      - CHANGELOG.md
      - LICENSE
      - README.md
  when:
    branch:
      - infra
    event:
      - push